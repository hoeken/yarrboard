Firmware:

* give ledcwrite and higher res pwm another chance
* investigate OTA update again.
* add fan pwm and tach stuff.
* some sort of POST/GET method for toggling pin state
* GET page for board status / config / stats / etc

Long term:

* send/receive json over serial as well
* control via N2K bus